# FastTD3 迁移一致性审查（2026-09-09）

## 修复进度（原审查快照保留在下方）

用户已明确批准保留 episode 记忆边界，作为相对 PPO 的实验差异，奖励裁剪必须保留。

本轮代码已恢复奖励 `[-1,1]` 裁剪、mask 步数计数、全向量等效更新及预热时钟、cuDNN benchmark、原 Rich dashboard/Utilization、SIGINT 保存和 logger 收尾；接回训练内 WOSAC 子进程，恢复本地 checkpoint 续训（重新初始化环境/replay）。算法 batch 独立为 `fasttd3.batch_size=32768`。长度元数据改为一次 CPU 拷贝，前缀 pack 后再编码以消除填充计算，未截断历史。

`reduce-overhead` 在微批累积时曾实际触发 CUDA Graph 梯度缓冲错误，预分配稳定梯度后通过 CUDA 更新测试，默认恢复官方模式。C51 的目标 MLP 使用 AMP，logits 后的投影维持 FP32；增加投影及损失区域编译。仍然是兼容 cuDNN LSTM 的分段编译，采样、packed LSTM、优化器调度不在单一更新图内。

尚不能宣布所有工程等价或性能验收完成：原统计中数据直方图与有效样本口径、rollout 收尾行为仍须继续核对；load-id 在线模型恢复和多 learner 未扩展，当前目标为单 GPU。完整规模、PyTorch 2.8/CUDA12.8/5090 的编译、显存、真实视频/WOSAC 与速度仍需服务器验证。下方矩阵描述修复前快照，当前实现以本段和 FASTTD3_MIGRATION.md 为准。

## 结论与范围

当前版本是复用了 Drive 环境和部分工具的独立 FastTD3-LSTM 训练器，还没有满足“保留 spiced_self_play 工程、只替换算法”的完整要求。之前的短测试证明部分路径能执行，不能证明工程等价、统计等价或性能达标。

本次只审查，不修改训练代码或参数。对照原始 Git 基线 `b930fd664682cdd3298d60fa36d90e7bb373cf13`、当前工作区全部修改及新增训练器、官方 FastTD3 本地源码（此前锁定 `229ed59bbf43ea2f7a2d5d90d1076314839944d7`），结合用户 AutoDL 日志。没有进行新的服务器性能测试，也没有直接核验 W&B 网页中的曲线。

## 为什么两个 epoch 就用了约 41 分钟

日志的两个记录点为 `524288 / updates=242 / SPS=463` 与 `1048576 / updates=498 / SPS=399`。按打印的整数 SPS 反推，两个区间共约 2446 秒。它不是整场运行时长证明；之后未完成的统计区间不会打印。

| 项目 | 原生 PPO | 当前 FastTD3 适配 |
|---|---|---|
| epoch | 完成一轮 rollout 和 PPO 更新 | `steps // 524288` 的报告区间 |
| 步数计数 | 每次 recv 累加 `mask.sum()` | 累加有上一次动作的槽位数，未扣当前 mask |
| 默认每轮学习 | 524288 / 32768 × 1 = 16 个 minibatch 更新 | 稳态每 524288 槽位约 256 次 critic、128 次 actor 更新 |
| 单次更新拆分 | 默认无需小微批梯度累积 | 32768 / 32 / 16 = 64 个微批 |
| 记忆计算 | rollout 窗口上的原生 LSTM | 每个微批多次重建历史，再计算训练窗口 |

因此 epoch 不是可直接比较的计算工作量，SPS 的计数口径也尚未严格统一。

明确的调度偏差：官方 `num_updates=2` 对应一次完整向量环境 step。当前配置有 8 个并行实例，每次异步返回 4 个；适配器在每次 recv 后执行 2 次更新。按全局并行槽位等效步对齐，相当于每完整步执行 4 次，扩大了两倍。`learning_starts` 和测量预热的时间单位也需要一起校正。应明确转换时间单位，不能通过随意把官方超参数改成 1 来掩盖。

明确的额外计算：每次 critic 更新的每个微批分别计算在线 actor、target critic、critic 的历史和序列；延迟 actor 更新再计算 actor 和 critic。前缀观测编码固定覆盖 110 步，包括无效填充；packed LSTM 只处理实际前缀。64 个 Python 微批循环、长度搬到 CPU、统计同步、重复编码都会产生开销。此前 15 分钟日志中，209 次更新约占 885 秒，后段 GPU 平均利用率约 10.55%；说明瓶颈集中在更新路径，但没有算子级剖析，不能给各原因分配确切占比。

## 从入口到退出的差异矩阵

| 环节 | 审查结果 | 处理原则 |
|---|---|---|
| 训练入口与生命周期 | `pufferl.train` 提前分流，绕过 `PuffeRL` 初始化、主循环及结束流程 | 恢复共享的 spiced 生命周期；只把采集存储与学习器等算法接口分离 |
| 地图、物理、奖励配置、并发配置 | drive.ini 原字段未修改，仅新增 fasttd3 段；连续动作通过 CLI 启用 | 保留；相同配置不代表整个运行语义已一致 |
| 连续动作 | delta_local 的 Box 从错误的 2 维修为 3 维；没有将二维地图误当成二维动作 | 已授权的必要接口修复 |
| 终止观测 | C 层额外捕获 reset 前观测；Python 传递回放元数据，包括地图重采样边界 | off-policy bootstrap 必需；保留原物理和奖励计算，单独测量传输成本 |
| 奖励预处理 | PPO `torch.clamp(r,-1,1)`；Fast 回放原始奖励 | 不能声称完全相同；核实实际越界奖励，并把是否保留裁剪列为明确决定 |
| 步数与有效样本 | global_step 计数不同；入库有效率不等于 PPO 训练 mask 比例 | 统一共有计数；另记 replay 入库率、训练有效率、更新数，不能同名异义 |
| LSTM | 保留原 Drive encoder 和 LSTM 尺寸，但 actor/critic/target 各自一套；采集状态重置边界改变 | 网络角色拆分属算法适配；记忆边界是实验变量，必须明确处理 |
| LSTM 边界细节 | PPO 每次 rollout 开始清零；Fast 跨报告区间保留状态，终止/重置清零，训练从 episode 前缀重建 | 不能把“保留 LSTM”解释成“记忆语义完全未变” |
| Replay | 自建完整 episode GPU 回放；结束后才能训练、全局按转移抽样、窗口填充及加权 | 与官方按并行环境存储/抽样的 transition replay 不等价；属于需说明和验证的 recurrent 适配 |
| 核心 FastTD3 | 官方 actor/C51 critic，在线 actor 生成目标动作、target critic、延迟 actor、Polyak、AdamW 等已接入 | 所锁定官方实现没有 target actor；不能按一般 TD3 印象另加。此结论不代表完整迁移已验收 |
| 算法参数 | 多数官方标量默认值已迁入；更新时间单位漂移；微批、前缀、replay 是适配设置 | 分别列出官方参数与适配参数，不能一概称“官方配置” |
| 归一化 | 使用官方 normalizer，保留道路类别 ID；recurrent 窗口重复观测参与更新统计 | 与官方逐转移统计频次不同；需要专门核验统计权重，不能只比较开关 |
| AMP | BF16 已启用；当前 target projection 连同目标 head 路径在 FP32 | 不是完全未实现 AMP；应确认 FP32 必需范围，不能为了加速牺牲 C51 正确性 |
| compile | 编码器和 head 局部编译；LSTM 与更新循环 eager；模式 default | 未等价迁移官方完整更新函数的 reduce-overhead 编译；必须报告兼容性限制与实测收益 |
| cuDNN | 原生 `benchmark=True` 在被绕过的构造器里，Fast 没有设置 | 原生工程设置遗漏，应恢复共用初始化 |
| 高并发流水线 | 仍调用原 load_env/异步 send-recv，但 recv 后插入大量同步学习 | 并发参数未变，环境推进与学习的实际节奏已变；需核验流水线等待 |
| 控制台 | 原 Rich dashboard、Utilization 线程未复用，替换成单行输出 | 属工程遗漏，应恢复，算法损失栏替换成 FastTD3 实际指标 |
| W&B | 复用 logger，后补 environment/losses/performance/data；并非完整复用原统计过程 | 恢复共有字段、统计来源、横轴、显示方式；PPO 专属 KL/entropy 等不能伪造为 FastTD3 指标 |
| 数据统计 | lambda/reward 直方图来自最后一次采集，区别于原训练采样来源；anchor_entropy 等未照搬 | 同名字段应保持含义，确实不适用者明确说明；λ0 阶段不虚构 anchor 训练 |
| HR/SP 评估与视频 | 调用原 Evaluator，适配确定性动作，修复原渲染调用问题 | 评估路径存在，不代表一小时已评估；默认每 500 epoch，当前尚未触发 |
| WOSAC、恢复训练、DDP | Fast 训练入口明确拒绝这些路径；独立评估不等于训练集成 | 当前 1 GPU 测试不触发全部限制，但不能称原工程功能完整保留 |
| 保存 | 已恢复编号 checkpoint、trainer_state、W&B artifact；格式适配 Fast 优化器等 | 恢复加载闭环和结束保存；只写 optimizer 状态不等于可恢复训练，replay/采集状态策略需说明 |
| 正常结束 | 缺少原收尾 rollout/drain；finally 不主动新存最后模型 | 需恢复工程收尾合同，明确最后 checkpoint 对应哪个训练步 |
| 一小时超时 | pufferl 顶层 SIGINT 调用 `os._exit(0)`，跳过 finally；默认 500 epoch 保存不够 | 之前 timeout SIGINT 命令不能保证最终模型和 W&B finish，必须修复再跑 |
| 其他修改 | W&B 新版 ID 兼容、PPO 专属 C 扩展延迟检查、评估 ob 更新/渲染参数修复 | 属兼容或既有 bug 修复，单独列项，不混称算法必需修改 |

## 修复边界与验收

1. 先统一共有生命周期、统计语义、Rich/W&B、评估保存和中断退出；保持原 PPO 路径可回归验证。无需再造第三套训练框架。
2. 校正 FastTD3 的全局向量步调度、预热单位；保留官方算法标量和原并发数量，报告实际 update-to-data ratio。
3. 保留 LSTM，优化重复编码、微批和同步；任何前缀截断、状态缓存、网络共享或采样分布变动都先说明其语义影响。不要直接用删记忆或改 batch 来换速度。
4. 单独敲定原奖励裁剪和 LSTM 记忆边界的保留方式，再改相关代码。原实现可能有缺陷不代表可以静默修正实验条件。
5. 先做少量目标测试：计数与调度、终止/截断序列、统计字段、最终保存和中断退出。服务器用短时分段计时确认瓶颈和 GPU 利用率后再跑一小时。

最终比较至少同时报告：相同定义的环境步数、有效转移数、墙钟时间、实际 critic/actor 更新数、训练样本量以及同一 HR/SP 评估成绩。epoch 数不足以支持速度或学习效果结论。现有运行只能作为适配器调试数据，不能归因于 FastTD3 算法本身。
